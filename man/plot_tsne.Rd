% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent_count.R
\name{plot_tsne}
\alias{plot_tsne}
\title{Plot observed and estimated latent counts on 2D using t-sne}
\usage{
plot_tsne(Y, Y_latent, opt_cl, data_names = NULL, color_pal = NULL)
}
\arguments{
\item{Y}{a list of matrices. Each matrix is a gene-by-cell matrix of mRNA counts corresponding to a dataset.}

\item{Y_latent}{a list of matrices, each is a gene-by-cell matrix of posterior mean of latent counts for a dataset.}

\item{opt_cl}{a list of vectors. Each vector stores the optimal clustering for one dataset.}

\item{data_names}{optional. The labels for each dataset in the plot.}

\item{color_pal}{optional. A vector of color names to map to clusters.}
}
\value{
two plots showing t-sne representation of the observed counts (first) and latent counts (second),
where observations are colored by optimal clustering, and shaped by different datasets.
}
\description{
Plot observed and estimated latent counts on 2D using t-sne
}
\examples{
plot_tsne(Y = list(t(y1), t(y2)), Y_latent = Y_latent, opt_cl = opt_cl, color_pal = c15)
}
