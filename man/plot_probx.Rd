% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probx_analysis.R
\name{plot_probx}
\alias{plot_probx}
\title{Plot posterior samples for covariate-dependent probabilities}
\usage{
plot_probx(
  post_result,
  opt_cl,
  x,
  mfrow = c(2, 2),
  data_names,
  xlab,
  thinning = NULL,
  truth = NULL,
  plot.empty.cluster = FALSE
)
}
\arguments{
\item{post_result}{output from \code{gkernelHDP_mcmc} or \code{pkernelHDP_mcmc} where clustering is fixed to the optimal one.}

\item{opt_cl}{optimal clustering used for the post-processing step.}

\item{x}{a list of vectors. Each vector denotes the covariate in one dataset.}

\item{mfrow}{parameter used to cut the plot window into subpanels}

\item{data_names}{the labels for each dataset in the plot.}

\item{xlab}{one string. The label for x-axis in the plot.}

\item{thinning}{if provided, apply thinning when plotting MCMC samples.}

\item{truth}{a list of matrices. Each matrix stores the covariate-dependent probabilities for one dataset,
where rows correspond to observations and columns correspond to clusters.}

\item{plot.empty.cluster}{if \code{FALSE}, only plot for occupied clusters.}
}
\value{
plots showing MCMC samples of covariate-dependent probabilities for every cluster in each dataset.
}
\description{
This function plots probabilities against the covariate based on the MCMC samples from the post-processing step.
}
\examples{
plot_probx(post_result = post_result, opt_cl = opt$opt_cl, x = list(t1, t2),
           mfrow = c(1,2), data_names = c('data1', 'data2'),
           xlab = 't', thinning = 5, truth = list(p_j1, p_j2))
}
