% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_marker_genes.R
\name{local_marker_genes}
\alias{local_marker_genes}
\title{Find local marker genes}
\usage{
local_marker_genes(
  post_result,
  threshold = c(2.5, 2.5),
  alpha_m = NULL,
  alpha_d = NULL,
  mc.cores = 8
)
}
\arguments{
\item{post_result}{output from \code{gkernelHDP_mcmc} for the post-processing step.}

\item{threshold}{a vector of length 2, corresponding to the threshold in the calculation of tail probabilities.}

\item{alpha_m}{optional. The threshold to classify locally differentially expressed (DE) genes. If not provided, the value will be
chosen to achieve an expected false discovery rate (EFDR) of 0.05.}

\item{alpha_d}{optional. The threshold to classify locally differentially dispersed (DD) genes. If not provided, the value will be
chosen to achieve an expected false discovery rate (EFDR) of 0.05.}

\item{mc.cores}{number of cores used for parallel computing.}
}
\value{
The output contains the following items:
\item{mu.output}{a dataframe containing mean absolute log-fold change, tail probabilities and classification of local DE genes for each cluster.}
\item{phi.output}{a dataframe containing mean absolute log-fold change, tail probabilities and classification of local DD genes for eachc luster.}
\item{alpha_m}{threshold to classify local DE genes.}
\item{alpha_d}{threshold to classify local DD genes.}
}
\description{
Identify locally differentially expressed and dispersed genes.
}
\examples{
local_result <- local_marker_genes(post_result = post_result,
  threshold = c(1.2,1.2), alpha_m = 0.3, alpha_d = 0.3, mc.cores = 2)
}
