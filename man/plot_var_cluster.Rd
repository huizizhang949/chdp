% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_var_cluster.R
\name{plot_var_cluster}
\alias{plot_var_cluster}
\title{Plot each feature against time with for time-series data and show clustering}
\usage{
plot_var_cluster(
  Y,
  t,
  opt_cl,
  data_names = NULL,
  color_pal = NULL,
  nrow_legend = 1
)
}
\arguments{
\item{Y}{a list of two matrices for observations. The columns correspond to features.}

\item{t}{a list of two vectors. Each vector is the external covariate (time) for individual dataset.}

\item{opt_cl}{a list of vectors. Each vector stores the optimal clustering for one dataset.}

\item{data_names}{optional. The labels for each dataset in the plot.}

\item{color_pal}{optional. A vector of color names to map to clusters.}
}
\value{
Line plots for each dataset, showing each feature against time, and clusters.
}
\description{
This function shows the line plots of each feature against time, with observations colored by clustering, for each dataset.
}
\examples{
plot_var_cluster(Y=list(Y1,Y2), t=list(t1,t2), opt_cl = opt_cl, color_pal = c25)
}
