% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent_count.R
\name{plot_mean_latent_count}
\alias{plot_mean_latent_count}
\title{Compute and plot posterior mean of covariate-dependent mean latent count}
\usage{
plot_mean_latent_count(
  post_result,
  t,
  gene_ix,
  prob = 0.95,
  xlab = NULL,
  data_names = NULL,
  gene_names = NULL,
  nrow = 1,
  mc.cores = 8
)
}
\arguments{
\item{post_result}{output from \code{gkernelHDP_mcmc} for the post-processing step.}

\item{t}{a list of covariates for each dataset.}

\item{gene_ix}{index of genes to plot.}

\item{prob}{the target probability content of the interval.}

\item{xlab}{string. The label for x-axis in the plot.}

\item{data_names}{the labels for each dataset in the plot.}

\item{gene_names}{the labels for each gene in the plot}

\item{nrow}{parameter used to cut the plot window into subpanels.}

\item{mc.cores}{number of cores used for parallel computing.}
}
\value{
a lineplot showing posterior mean of mean latent count in black, and highest
posterior density interval in red dashed line. Also return a list of arrays for each dataset.
Each array is of the form \eqn{a[c,g,i]} where \eqn{c}, \eqn{g}, \eqn{i} correspond to cell, gene and
statistics (mean, lower bound, upper bound), respectively.
}
\description{
This function computes posterior mean and highest posterior density interval of
mean latent count, and plots it versus covariate
}
\examples{
plot_mean_latent_count(post_result = post_result, t = list(t1, t2), gene_ix = c(1,2),
                       prob = 0.99, xlab='t', mc.cores = 4)
}
