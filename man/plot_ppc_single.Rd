% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppc_single.R
\name{plot_ppc_single}
\alias{plot_ppc_single}
\title{Posterior predictive checks based on one single replicate}
\usage{
plot_ppc_single(post_result, Y, opt_cl, data_names = NULL)
}
\arguments{
\item{post_result}{output from \code{gkernelHDP_mcmc} for the post-processing step.}

\item{Y}{a list of matrices. Each matrix is a gene-by-cell matrix of mRNA counts corresponding to a dataset.}

\item{opt_cl}{a list of vectors. Each vector stores the optimal clustering for one dataset.}

\item{data_names}{optional. The labels for each dataset in the plot.}
}
\value{
two plots. First plot compares the relationship between mean and standard deviation of log shifted counts,
and between log of mean counts and dropout probabilities, for observed (red) and replicated (black) data.
Second plot compares each statistic between the observed and replicated data.
}
\description{
The function generates one replicated dataset and compare relationships between statistics for
observed and replicated data, as well as differences in statistics between observed
and replicate data.
}
\examples{
plot_ppc_single(post_result = post_result, Y = list(t(y1), t(y2)),
                opt_cl = opt_cl, data_names = c('data 1', 'data 2'))
}
